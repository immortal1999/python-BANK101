
#to be run by Docker

#Web Framework
fastapi==0.136.3
uvicorn[standard]==0.49.0

# Database
SQLAlchemy==2.0.50
alembic==1.18.4
psycopg2-binary==2.9.9        # PostgreSQL driver (RDS); not needed for local SQLite

# Config / validation
pydantic==2.13.4
pydantic-settings==2.14.1
python-dotenv==1.2.2

# AWS (Secrets Manager)
boto3==1.35.0

# Auth / security
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0
