{
  "_comment": "Template for the AWS Secrets Manager secret 'banking-app'. Copy to secrets.json, fill in REAL values, create the secret, then delete secrets.json. NEVER commit the real one.",

  "JWT_SECRET_KEY": "<generate: python3 -c \"import secrets; print(secrets.token_urlsafe(32))\">",

  "POSTGRES_USER": "dbadmin",
  "POSTGRES_PASSWORD": "<tier-1 superuser password — token_urlsafe(32)>",
  "POSTGRES_DB": "bank101",

  "BANKADMIN_PASSWORD": "<tier-2 schema/migration role password — token_urlsafe(32)>",
  "BANKAPP_PASSWORD": "<tier-3 app runtime role password — token_urlsafe(32)>",

  "DATABASE_URL": "postgresql://bankapp:<BANKAPP_PASSWORD>@db:5432/bank101",
  "ALEMBIC_DATABASE_URL": "postgresql://bankadmin:<BANKADMIN_PASSWORD>@db:5432/bank101",

  "DOMAIN": "your-subdomain.duckdns.org"
}
